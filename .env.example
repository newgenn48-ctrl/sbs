# SMTP — contactformulier-mail
SMTP_HOST=smtp.example.com
SMTP_PORT=465
SMTP_USER=no-reply@example.com
SMTP_PASS=replace-with-real-secret
SMTP_FROM=no-reply@example.com
SMTP_TO=info@startbeheer.nl

# Bundle analyzer (optioneel)
ANALYZE=false
